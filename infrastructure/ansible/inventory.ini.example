[trading]
# Replace with your server's public IP (from Terraform output)
# Update ansible_ssh_port to match your custom SSH port
YOUR_SERVER_IP ansible_user=root ansible_ssh_private_key_file=~/.ssh/your_key ansible_ssh_port=YOUR_SSH_PORT
