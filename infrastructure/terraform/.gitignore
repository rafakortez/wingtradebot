# Terraform state files
*.tfstate
*.tfstate.backup
.terraform/

# Credentials files - NEVER commit these
# Use environment variables: TF_VAR_* instead
*.json
*.tfvars

# .terraform.lock.hcl should be committed (locks provider versions)
