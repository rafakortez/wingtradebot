terraform.tfstate
terraform.tfstate.backup
.terraform/
terraform.tfvars

# GCP Service Account JSON - CONTAINS SENSITIVE CREDENTIALS
*.json
!package.json

# .terraform.lock.hcl deve ser commitado (trava versão dos providers)
